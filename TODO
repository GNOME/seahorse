0.4.x:
	help:
		-updates for new
	key-properties:
		-use labels if can't modify data
		-hide unecessary data
	context:
		-gconf listener for changes
	package:
		-restructure for gnome cvs and future plugins
	context & key:
		-check reference counting of each & gpgme datas
	translations:
		-spanish
		-german

0.6.x:
	key-properties & key-ops:
		-add user id
		-delete user id
		-set uid as primary
		-add sub key
		-delete sub key
		-edit sub key expire
	widget:
		-allow only one of each type of widget
	key-widget:
		-allow only one of each type of key-widget per key
	data-ops:
		-encrypt & sign
	context:
		gconf error checking

0.7.x:
	key-manager:
		-sorting
		-drag in = import
		-drag out = export
		-popup key menu
		-visible if has secret
	recipients:
		-sorting
		-drag between sides
	key:
		-nice fingerprint
	text-editor:
		-icons for popups
	file-manager:
		-listing and/or toolbar
	command-line:
		-file ops
	nautilus:
		-component

0.8.x:
	key-signatures:
		-reqs gpgme-0.4
		-listing
		-creating
